.img-container {
  position: relative
  overflow: hidden
  
  img {
    a & {
      transform: scale(1)
      transition(transform, 1s)
    }
    
    a:hover & {
      transform: scale(1.1)
    }
  }
}