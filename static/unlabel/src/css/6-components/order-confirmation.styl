.order-confirmation {}

.order-confirmation__successMessage {
  margin-top: 42px
}

.order-confirmation__instructions {
  margin-top: 42px
}

.order-confirmation__actions {
  margin-top: 42px
  .cta {
    @media $mq_s_only {
      display: block
      width: 80%
      margin: 0 auto
    }
    
    & + .cta {
      @media $mq_s_only {
        margin-top: 12px
      }
      @media $mq_m {
        margin-left: 12px
      }
    }
  }
}